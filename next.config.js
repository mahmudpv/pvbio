/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['assets.raribleuserdata.com', 'nft-cdn.alchemy.com'],
  },
}

module.exports = nextConfig
